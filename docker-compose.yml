services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3010:3010"
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - PORT=3010
    volumes:
      - kairos_data:/app/data
    restart: always

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3008:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://187.77.229.227:3010/api
    depends_on:
      - backend
    restart: always

volumes:
  kairos_data:
